import React from 'react';
import { Typography } from 'antd'
const { Title } = Typography;

export default function Subheader() {
    return (
        <div style={{padding: '30px', background: '#007bff'}}>
            <Title>Questions</Title>
        </div>
    )
}