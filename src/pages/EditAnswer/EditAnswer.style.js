import styled from 'styled-components';

const EditAnswerWrapper = styled.div`
    @media(max-width: 768px) {
        .form-card {
            border: none;
        }
        .title {
            padding-left: 24px;
        }
    }
`

export default EditAnswerWrapper;